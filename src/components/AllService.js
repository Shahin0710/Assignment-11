import { Typography } from '@mui/material';
import React from 'react';
import ComponentsLayout from './ComponentsLayout';

const AllService = function () {
    return (
        <ComponentsLayout>
            <Typography variant="h1" color="error" align="center">
                AllService
            </Typography>
        </ComponentsLayout>
    );
};

export default AllService;
